number := import("number");

String := func str: str:
	str := {
		root := str,
		len := number.Number(0)
	};
	root := str.root;
	loop
		if root == nil then
			exit;
		else
			str.len:$add(number.number(1));
			root := root.$next;
		end
	end
end;

$module := {
	String := String
};

